// const assert = require("assert");

/**
 * Write a function that takes an object and a property,
 * and returns the value of that property
 * @param {object} object
 * @param {string} property
 */
function getPropertyValue(object, property) {

    let myObject = {} 
    }
    

    
    
           
        
        return myProperty[myObject]

  // TODO
  // hint: object properties can be accessed with square brackets
  // this syntax `myThing[myProperty]` is the same as `myThing.myProperty`


console.log(getPropertyValue({ test: "a" }))

// assert.strictEqual(
//   getPropertyValue({ test: "a" }, "test"),
//   "a",
//   "Expected to get 'a' but we didn't!"
// );
// assert.strictEqual(
//   getPropertyValue({ name: "eduardo" }, "name"),
//   "eduardo",
//   "Expected to get 'eduardo' but we didn't!"
// );
// assert.strictEqual(
//   getPropertyValue({ age: 23, species: "monkey" }, "species"),
//   "monkey",
//   "Expected to get 'monkey' but we didn't!"
// );