// Write a function that when given a URL as a string, parse out just the domain name and return as a string. For Example:

//fnc -> str url -> str domain
//url -> http/s: no funny biz

function parseDomainName(url) {
    //replace the stuff from we don't need ""
    //split and grab the domain 
}
console.log("https://github.com/Layloe/hvac/blob/main/views/index.html")