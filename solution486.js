// The aim of this kata is to split a given string into different strings of equal size (note size of strings is passed to the method)

// Example:

// Split the below string into other strings of size #3

// 'supercalifragilisticexpialidocious'

// Will return a new string
// 'sup erc ali fra gil ist ice xpi ali doc iou s'
// Assumptions:

// String length is always greater than 0
// String has no spaces
// Size is always positive

function splitInParts (s, partLength){
    const whiteSpace = 
    return 
  }
  console.log(splitInParts("supercalifragilisticexpialidocious"))
// Input has two parameters one is a  string of words of letters the other a number.
// Output is a the same string but with spaces depending on the second parameter.
//   assert.equal(splitInParts("supercalifragilisticexpialidocious", 3), "sup erc ali fra gil ist ice xpi ali doc iou s");
//   assert.equal(splitInParts("HelloKata", 9), "HelloKata");
//   assert.equal(splitInParts("HelloKata", 1), "H e l l o K a t a");
// Skipping this one but I believe it's solved with hash 
